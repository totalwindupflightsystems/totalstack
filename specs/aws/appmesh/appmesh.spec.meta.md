"""App Mesh meta spec — provides architecture overview for the App Mesh service emulator.

Entity hierarchy: Mesh (root) → VirtualNode, VirtualService, VirtualRouter (peers)
                               VirtualRouter → Route (child)
"""
