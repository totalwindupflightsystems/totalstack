name: Triage Stale Issues
# DISABLED for TotalStack fork — references upstream localstack/meta
# reusable workflow and requires PRO_ACCESS_TOKEN not available here.

on:
  workflow_dispatch:  # manual only — cron removed

jobs:
  sync-with-project:
    if: false  # disabled — upstream workflow not applicable to fork
    uses: localstack/meta/.github/workflows/stale-bot.yml@main
    secrets:
      github-token: ${{ secrets.PRO_ACCESS_TOKEN }}
